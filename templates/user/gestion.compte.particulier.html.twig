{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit User</h1>

        {{ include('user/_form.html.twig', {'button_label': 'Mise à Jour'}) }}

        <button class="btn orange"><a href="{{ path('index') }}">Retour à l'acceuil</a></button>
        <div class="aligner_button">
            {{ include('user/_delete_form.html.twig') }} 
        </div>
    {% endblock %}
