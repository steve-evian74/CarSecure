{% extends 'base.html.twig' %}

{% block title %}CarnetEntretien index{% endblock %}

{% block body %}
    <h1>CarnetEntretien index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateIntervention</th>
                <th>Kilometre</th>
                <th>CommentaireIntervention</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carnet_entretien in carnet_entretiens %}
            <tr>
                {{dump(carnet_entretien)}}
                <td>{{ carnet_entretien.id }}</td>
                <td>{{ carnet_entretien.dateIntervention ? carnet_entretien.dateIntervention|date('Y-m-d') : '' }}</td>
                <td>{{ carnet_entretien.kilometre }}</td>
                <td>{{ carnet_entretien.commentaireIntervention }}</td>
                <td>
                    <a href="{{ path('carnet_entretien_show', {'id': carnet_entretien.id}) }}">show</a>
                    <a href="{{ path('carnet_entretien_edit', {'id': carnet_entretien.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('carnet_entretien_new') }}">Create new</a>
{% endblock %}
